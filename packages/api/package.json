{
  "name": "@finmath/api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "echo 'API build skipped (no src/)' && exit 0",
    "dev": "echo 'API dev skipped' && exit 0",
    "start": "echo 'API start skipped' && exit 0",
    "lint": "echo 'API lint skipped (no src/)' && exit 0",
    "typecheck": "echo 'API typecheck skipped (no src/)' && exit 0",
    "test": "echo 'API tests skipped' && exit 0"
  },
  "dependencies": {
    "@finmath/engine": "workspace:*",
    "express": "^4.21.2",
    "zod": "^3.24.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.24",
    "@types/cors": "^2.8.17",
    "typescript": "^5.5.3"
  }
}
