{
  "name": "@finmath/engine",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "echo 'Engine build skipped (no src/)' && exit 0",
    "test": "echo 'Engine tests skipped (no tests/)' && exit 0",
    "test:watch": "echo 'Engine tests skipped' && exit 0",
    "test:golden": "echo 'Engine golden tests skipped' && exit 0",
    "test:coverage": "echo 'Engine coverage skipped' && exit 0",
    "lint": "echo 'Engine lint skipped (no src/)' && exit 0",
    "typecheck": "echo 'Engine typecheck skipped (no src/)' && exit 0"
  },
  "dependencies": {
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.19.21",
    "typescript": "^5.5.3",
    "vitest": "^2.0.0"
  }
}
